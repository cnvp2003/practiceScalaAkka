import scala.concurrent.Future

/**
  * Created by pati80 on 15/09/2018.
  */



object Flex extends App {

  /*val restEndPoint = "http://myserver.com/api/"
  val url = s"${restEndPoint}forecast/${year}"
  val result = scala.io.Source.fromURL(url).
  println(result)*/



/*  import dispatch._, Defaults._
  import scala.util.{Success, Failure}

  object Test {

    def main (args: Array[String]) {
      val svc = url("http://www.wikipedia.org/")
      val response : Future[String] = Http()

      response onComplete {
        case Success(content) => {
          println("Successful response" + content)
        }
        case Failure(t) => {
          println("An error has occurred: " + t.getMessage)
        }
      }
    }
  }*/
}